@{
    ViewBag.Title = "Wählen";
    ViewBag.Description = "Stimmabgabe für die Bundestagswahl";
}

<div class="row">
    <div class="padding-bottom-md">
        Bitte geben Sie ihren Zugangscode in das Textfeld ein und bestätigen sie diesen mit "Weiter", so dass Sie ihre Stimme für diese Wahl abgeben können.
    </div>

    <div class="padding-top-md padding-bottom-md">
        @using (Html.BeginForm("Elect", "Vote"))
        {
            <strong class="margin-left-lg">Token:</strong>
            @Html.TextBox("txtToken")
            <input type="submit" value="Weiter" class="btn btn-primary margin-left-lg"/>

            @Html.ActionLink("Token generieren", "GenerateToken", "Vote", null, new { @class="btn btn-primary margin-left-lg"})
        }
    </div>

    <div class="margin-top-lg">
        <strong>Wichtig:</strong>
        Ihr Token berechtigt Sie nur zu einer einzigen Stimmabgabe.
    </div>
</div>
